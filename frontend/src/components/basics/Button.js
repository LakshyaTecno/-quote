import React from "react";
import "./style.css";

const Button = ({Api}) => {
  

  return (
    <>
    <button className="btn-group btn-group__item ">Filter</button>
    </>
  );
};

export default Button;